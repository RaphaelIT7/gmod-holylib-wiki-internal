<function name="DISABLE_GMODJIT" parent="" type="define">
	<description>
		If defined **BEFORE** the `#include "lua.h"` then HolyLib's JIT includes will be used instead of GMod's JIT includes.
	</description>
	<realm>Shared and Menu</realm>
</function>